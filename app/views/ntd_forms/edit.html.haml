.row
  .col-md-6.col-md-offset-3
    %fieldset
      %legend Update NTD Form
      = render(:partial => 'shared/form_errors', :locals => {:obj => @form}) unless @form.errors.empty?

      = render 'form'

= render 'scripts'
