<%= form_for(@tam_policy) do |f| %>
  <% if @tam_policy.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tam_policy.errors.count, "error") %> prohibited this tam_policy from being saved:</h2>

      <ul>
      <% @tam_policy.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
