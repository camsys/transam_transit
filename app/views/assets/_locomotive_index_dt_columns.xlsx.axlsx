[a.serial_number, a.gross_vehicle_weight, a.fta_ownership_type, a.fta_vehicle_type, a.fuel_type, a.title_number, a.title_owner, a.rebuild_year, a.vehicle_storage_method_type].each do |val|
  row.add_cell(val)
end
